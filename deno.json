{
  "tasks": {
    "dev": "deno run -A main.ts --dev",
    "start": "deno run -A main.ts",
    "check": "deno check main.ts && deno check scripts/generate.ts",
    "generate": "deno run -A generate.ts"
  },
  "fmt": {
    "proseWrap": "never",
    "singleQuote": true,
    "semiColons": false,
    "exclude": ["components/markup"]
  }
}
